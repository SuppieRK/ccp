[
  {
    "name": "failure",
    "tool": "npx",
    "project": "projects/basic",
    "before_start": [["./scripts/setup.sh"]],
    "after_stop": [["./scripts/cleanup.sh"]],
    "native": ["npx", "eslint", "src/lint_fail.js"],
    "expect_exit": 1,
    "must_contain": ["lint_fail.js", "no-unused-vars", "semi"],
    "must_not_contain": [],
    "ignore_lines": ["^$"],
    "required": true
  },
  {
    "name": "success",
    "tool": "npx",
    "project": "projects/basic",
    "before_start": [["./scripts/setup.sh"]],
    "after_stop": [["./scripts/cleanup.sh"]],
    "native": ["npx", "eslint", "src/lint_ok.js"],
    "expect_exit": 0,
    "must_contain": [],
    "must_not_contain": ["Need to install the following packages:", "Ok to proceed?"],
    "ignore_lines": ["^$"],
    "required": false
  }
]
